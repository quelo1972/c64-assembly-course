# Lezione 002 - Sistema binario ed esadecimale

Esempio minimo che carica un valore esadecimale e lo scrive sul registro del bordo.

Compilazione:

```bash
64tass --cbm-prg -o ../../bin/002-binary-hex.prg main.asm
```
