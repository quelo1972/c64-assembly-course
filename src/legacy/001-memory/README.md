# Lezione 001 - La memoria del Commodore 64

Esempio minimo che mostra scrittura su un registro video (`$D020`).

Compilazione:

```bash
64tass --cbm-prg -o ../../bin/001-memory.prg main.asm
```

Obiettivo: cambiare il colore del bordo scrivendo in memoria.
