# Lezione 006 - Dal sorgente all'esecuzione

Esempio minimo e test di assemblaggio per verificare il percorso sorgente → PRG.

Compilazione:

```bash
64tass --cbm-prg -o ../../bin/006-from-source-to-execution.prg main.asm
```
